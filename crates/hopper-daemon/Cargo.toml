[package]
name = "hopper-daemon"
description = "Binary: tokio daemon + OpenAI-compatible HTTP facade (mirrors reference/api.py)."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
